#ifndef SORTER_HELPER
#define SORTER_HELPER

	// Declaring functiosn to be used in sorter.cpp
	void bubbleSort(int numberList[], bool sortAscended, int size);
	void viewNumbers(int numList[], int size);

#endif